% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oe_layer.R
\name{oe_layer}
\alias{oe_layer}
\alias{get_oe}
\title{Access a One Earth Bioregions Framework layer}
\source{
One Earth Bioregions Framework (2023), \url{https://www.oneearth.org},
licensed CC BY-NC 4.0 (non-commercial use, cite One Earth);
Ecoregions: Dinerstein et al. (2017) \doi{10.1093/biosci/bix014},
licensed CC-BY 4.0.
}
\usage{
oe_layer(
  layer = c("realms", "subrealms", "bioregions", "ecoregions"),
  scale = c("small", "medium", "large"),
  returnclass = c("sv", "sf"),
  realm = NULL,
  subrealm = NULL,
  bioregion = NULL,
  name = NULL
)

get_oe(
  layer = c("realms", "subrealms", "bioregions", "ecoregions"),
  scale = c("small", "medium", "large"),
  returnclass = c("sv", "sf"),
  realm = NULL,
  subrealm = NULL,
  bioregion = NULL,
  name = NULL
)
}
\arguments{
\item{layer}{One of \code{"realms"}, \code{"subrealms"}, \code{"bioregions"},
\code{"ecoregions"}. Partial matching is allowed.}

\item{scale}{Scale of the geometries: \code{"small"}, \code{"medium"} or \code{"large"}
(numeric equivalents \code{110}, \code{50}, \code{10} as in Natural Earth are accepted).
\code{"small"} returns the packaged simplified layer; \code{"medium"}/\code{"large"}
download the full-resolution geometries.}

\item{returnclass}{Spatial class of the returned object: \code{"sv"} for a
\code{terra} \code{SpatVector} (default) or \code{"sf"} for a simple feature object
(requires the \code{sf} package).}

\item{realm, subrealm, bioregion}{Optional character vectors of framework
names to filter the result (e.g. \code{realm = "Neotropic"},
\code{subrealm = "Amazonia"}, \code{bioregion = "Amazon River Estuary"} or a code
such as \code{"NT04"}).}

\item{name}{Optional character vector of feature names to keep
(\code{eco_name} for ecoregions, \code{subrealm_n} for subrealms, realm or
bioregion names for the other layers).}
}
\value{
A \code{terra::SpatVector} (or \code{sf} object) of polygons in EPSG:4326.
}
\description{
Returns one layer of the One Earth Bioregions Framework, in the spirit of
\code{\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries()}}: pick the layer, the \code{scale} of the
geometries, optionally filter by framework names, and choose the spatial
class of the result.
}
\details{
All layers ship with the package as lightweight simplified geometries
(\code{scale = "small"}) so they work offline. \code{scale = "large"} (or
\code{"medium"}, currently equivalent) downloads the original geometries: the
ecoregions (~150 MB) come from the public RESOLVE cloud bucket, the other
layers from the One Earth shared folders. Downloaded files are cached in
\code{tools::R_user_dir("oneearthr", "cache")}.

The packaged geometries (\code{scale = "small"}) are simplified with
\code{\link[terra:simplifyGeom]{terra::simplifyGeom()}} (\code{preserveTopology = TRUE}): vertex positions may
deviate from the originals by up to ~0.02 degree (realms, subrealms,
bioregions) or ~0.15 degree (ecoregions). They are suitable for
overview maps and quick exploration, but not for precise area
calculations or boundary-exact spatial joins — use \code{scale = "large"}
for that.
}
\examples{
realms <- oe_layer("realms")
terra::plot(realms, col = as.factor(realms$biogeorelm))

# only the Neotropic ecoregions
nt <- oe_layer("ecoregions", realm = "Neotropic")
}
